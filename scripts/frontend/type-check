#!/bin/bash
# Usage: ./scripts/frontend/type-check
# Runs TypeScript type checking without emitting files.
# Good to run before committing to catch type errors that ESLint misses.

set -e

cd "$(dirname "$0")/../.."

echo "Type checking frontend..."
docker-compose run --rm client pnpm type-check

echo "✅ Type check passed."
