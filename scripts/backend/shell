#!/bin/bash
# Usage: ./scripts/backend/shell
# Opens the Django shell inside the web container.
# Useful for quick model queries and debugging.

set -e

cd "$(dirname "$0")/../.."

docker-compose run --rm web python manage.py shell
