#!/bin/bash
# Usage: ./scripts/backend/check
# Runs Django's system check framework.
# Good to run after model or settings changes before starting the server.

set -e

cd "$(dirname "$0")/../.."

echo "Running Django system check..."
docker-compose run --rm web python manage.py check

echo "✅ System check passed."
